import React from 'react'

function MaintainShops() {
  // 1. config sisse uus URL, mille lõpp on /shop.json
  // 2. lisada uusi poode andmebaasi
  //      4 refi - nameRef, openTimeRef, latitudeRef, longitudeRef
  // 3. kuvada andmebaasist
  //            {element.name} 
  //             {element.openTime} 
  //             {element.latitude} 
  //             {element.longitude} 
  // 4. kustutada andmebaasist

  // TEEKS PIGEM KOOS, aga kui tahate võite proovida:
  // Shops.jsx sisse

  return (
    <div>MaintainShops</div>
  )
}

export default MaintainShops